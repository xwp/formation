/**
 * Formation field setting extension.
 */

import { withFilters } from '@wordpress/components';

const FormationFieldSettings = () =>( <></> );

export default withFilters( 'FormationFieldSettings' )( FormationFieldSettings );

