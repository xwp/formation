/**
 * Formation field input extension.
 */

import { withFilters } from '@wordpress/components';


const FormationFieldInput = () =>( <></> );

export default withFilters( 'FormationFieldInput' )( FormationFieldInput );
